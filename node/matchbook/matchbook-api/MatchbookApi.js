function MatchbookApi(username, password) {
    this.username = username;
    this.password = password;

    this.login = function () {
        //check if session exist if doesn't exist login
    }
}

module.exports = {
    MatchbookApi,
};